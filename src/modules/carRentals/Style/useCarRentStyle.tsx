import { createStyles } from "@mantine/core";

const useCarRentStyle = createStyles(() => {
  return {
    inputBox: {
      width: "80%",
      margin: "0 auto",
    },
    submit: {
      color: "#000",
      backgroundColor: "#FFC527",
    },
  };
});
export default useCarRentStyle;
